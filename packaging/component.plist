<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<array>
    <dict>
        <!-- Critical: disables macOS Installer's bundle-relocation
             feature. Without this, if any XT-Forge.app already exists
             on the operator's machine (a prior install, or the dev's
             own dist/XT-Forge.app), the Installer redirects the
             payload to update THAT bundle instead of putting a fresh
             copy in the pkg's declared install-location. The
             postinstall then can't find the app where it expects and
             the install fails. -->
        <key>BundleIsRelocatable</key>
        <false/>
        <key>BundleIsVersionChecked</key>
        <true/>
        <key>BundleHasStrictIdentifier</key>
        <true/>
        <key>BundleOverwriteAction</key>
        <string>upgrade</string>
        <key>RootRelativeBundlePath</key>
        <string>XT-Forge.app</string>
    </dict>
</array>
</plist>
